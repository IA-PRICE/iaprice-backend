spring:
  application:
    name: iaprice-backend

  datasource:
    url: jdbc:postgresql://aws-0-eu-west-1.pooler.supabase.com:5432/postgres?sslmode=require
    username: postgres.scfofarcqvtjhvwsgwcm
    password: WPMxWO2qsDP96pDS
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 5
      connection-timeout: 20000

  jpa:
    hibernate:
      ddl-auto: validate        # Flyway gère le schéma, JPA valide uniquement
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

server:
  port: 8080

# ── JWT ──────────────────────────────────────────
jwt:
  secret: CHANGE_ME_MIN_32_CHARS_SECRET_KEY_HERE   # à remplacer par une vraie clé en prod
  expiration: 86400                                  # 24h en secondes

# ── Logging ──────────────────────────────────────
logging:
  level:
    ma.iaprice: DEBUG
    org.springframework.security: INFO
    org.flywaydb: INFO
